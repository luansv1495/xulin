export enum InfoMessage {
  loadConfig = 'Loading config...',
  removeErrorLog = 'Removing error log file....',
  execRules = 'Executing rules...'
}

export enum ErrorMessage {
  configNotFound = 'ENOENT: no such file or directory, open'
}
